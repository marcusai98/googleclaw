version: "3.9"

services:
  googleclaw:
    build: .
    container_name: googleclaw
    ports:
      - "8080:8080"
    volumes:
      - googleclaw_data:/app/data
    restart: unless-stopped
    environment:
      - GC_HOST=0.0.0.0

volumes:
  googleclaw_data:
